{
  "name": "can-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based viewer for CAN signal database JSON files.",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
